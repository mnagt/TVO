<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- New intermediate group: Team Documents Only -->
    <record id="group_sale_team_manager" model="res.groups">
        <field name="name">User: Team Documents Only</field>
        <field name="comment">The user will have an access to the documents of the sales teams he/she belongs to.</field>
        <field name="category_id" ref="base.module_category_sales_sales" />
        <field name="implied_ids" eval="[(4, ref('sales_team.group_sale_salesman'))]" />
    </record>

    <!-- Wire the new group into the hierarchy: All Documents now implies Team Documents -->
    <record id="sales_team.group_sale_salesman_all_leads" model="res.groups">
        <field name="implied_ids" eval="[(6, 0, [ref('group_sale_team_manager')])]" />
    </record>

    <data noupdate="1">
        <record id="sale_order_team_rule" model="ir.rule">
            <field name="name">Team Orders</field>
            <field name="model_id" ref="sale.model_sale_order" />
            <field name="domain_force">['|', '|', ('user_id', '=', user.id), ('user_id', '=', False), '|', ('team_id', '=', user.sale_team_id.id), ('team_id', '=', False)]</field>
            <field name="groups" eval="[(4, ref('group_sale_team_manager'))]" />
        </record>

        <record id="sale_order_line_team_rule" model="ir.rule">
            <field name="name">Team Order Lines</field>
            <field name="model_id" ref="sale.model_sale_order_line" />
            <field name="domain_force">['|', '|', ('salesman_id', '=', user.id), ('salesman_id', '=', False), '|', ('order_id.team_id', '=', user.sale_team_id.id), ('order_id.team_id', '=', False)]</field>
            <field name="groups" eval="[(4, ref('group_sale_team_manager'))]" />
        </record>
    </data>
</odoo>
