<?xml version="1.0" encoding="utf-8" ?>

<odoo>
    <data>


        <record id="view_partner_form_inherit_custom_button" model="ir.ui.view">
            <field name="name">res.partner.form.inherit.custom.button</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <xpath expr="//div[@name='button_box']/*[1]" position="before">
                    <button name="action_view_move_line_report"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-book"
                        groups="partner_balance.group_partner_balance_user">
                        <div class="o_stat_info">
                            <span class="o_stat_value">
                                <field name="partner_balance" widget="monetary"
                                    options="{'currency_field': 'currency_id'}"/>
                            </span>
                            <span class="o_stat_text">Balance</span>
                        </div>
                    </button>
                </xpath>
            </field>
        </record>



        <!-- Tree View for Account Move Line Normal Report -->
        <record id="view_account_move_line_report_tree" model="ir.ui.view">
            <field name="name">account.move.line.report.tree</field>
            <field name="model">account.move.line.report</field>
            <field name="arch" type="xml">
                <list string="Partner Ledger" create="false" delete="false" edit="false" js_class="partner_balance" decoration-info="line_type == 'product'">
                    <field name="date"/>
                    <field name="type"/>
                    <field name="reference"/>
                    <field name="note"/>
                    <field name="debit"/>
                    <field name="credit"/>
                    <field name="cumulated_balance" string="Balance"/>
                    <field name="company_currency_id" column_invisible="True"/>
                    <field name="amount_currency" string="Origin Amount"/>
                    <field name="currency_id" column_invisible="True"/>
                    <field name="line_type" column_invisible="True"/>
                    <field name="type_key" column_invisible="True"/>
                    <field name="move_id" column_invisible="True"/>
                </list>
            </field>
        </record>


        <!-- Tree View for Account Move Line TRY Report -->
        <record id="view_account_move_line_report_tr_tree" model="ir.ui.view">
            <field name="name">account.move.line.report.tr.tree</field>
            <field name="model">account.move.line.report</field>
            <field name="arch" type="xml">
                <list string="TL Value Report" create="false" delete="false" edit="false" js_class="partner_balance" decoration-info="line_type == 'product'">
                    <field name="date"/>
                    <field name="type"/>
                    <field name="reference"/>
                    <field name="note"/>
                    <field name="amount_tr_debit" string="Debit"/>
                    <field name="amount_tr_credit" string="Credit"/>
                    <field name="cumulated_amount_tr_currency" string="Balance"/>
                    <field name="tr_currency_id" column_invisible="True"/>
                    <field name="company_currency_id" column_invisible="True"/>
                    <field name="amount_currency" string="Origin Amount"/>
                    <field name="tr_rate_display" string="Rate"/>
                    <field name="currency_id" string="Currency" column_invisible="True"/>
                    <field name="line_type" column_invisible="True"/>
                    <field name="type_key" column_invisible="True"/>
                    <field name="move_id" column_invisible="True"/>
                </list>
            </field>
        </record>

         
        

        <record id="action_partner_move_line_tr_value" model="ir.actions.act_window">
            <field name="name">Statement in TRY</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">account.move.line.report</field>
            <field name="view_mode">list</field>
            <field name="view_id" ref="partner_balance.view_account_move_line_report_tr_tree"/>
            <field name="views" eval="[(ref('partner_balance.view_account_move_line_report_tr_tree'), 'list')]"/>
            <field name="target">current</field>
            <field name="domain">[('partner_id', '=', active_id), ('line_type', '=', 'summary')]</field>
            <field name="context">{'search_default_group_by_account': 1, 'report_type': 'ledger'}</field>
        </record>
        
        <record id="action_partner_move_line_usd" model="ir.actions.act_window">
            <field name="name">Statement of Account</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">account.move.line.report</field>
            <field name="view_mode">list</field>
            <field name="view_id" ref="partner_balance.view_account_move_line_report_tree"/>
            <field name="views" eval="[(ref('partner_balance.view_account_move_line_report_tree'), 'list')]"/>
            <field name="target">current</field>
            <field name="domain">[('partner_id', '=', active_id), ('line_type', '=', 'summary')]</field>
            <field name="context">{'search_default_group_by_account': 1, 'report_type': 'ledger'}</field>
        </record>

        <record id="view_account_ledger_balance_search" model="ir.ui.view">
            <field name="name">account.ledger.balance.search</field>
            <field name="model">account.ledger.balance</field>
            <field name="arch" type="xml">
                <search>
                    <field name="user_id" string="Salesperson"/>
                    <group expand="1" string="Group By">
                        <filter name="group_by_salesperson"
                                string="Salesperson"
                                context="{'group_by': 'user_id'}"/>
                    </group>
                </search>
            </field>
        </record>

        <record id="view_account_ledger_balance_tree" model="ir.ui.view">
            <field name="name">account.ledger.balance.tree</field>
            <field name="model">account.ledger.balance</field>
            <field name="arch" type="xml">
                <list string="Ledger Balance" create="false" delete="false" edit="false" js_class="ledger_balance">
                    <field name="partner_id" string="Partner"/>
                    <field name="user_id" optional="show" string="Salesperson"/>
                    <field name="balance" widget="monetary" options="{'currency_field': 'company_currency_id'}"/>
                    <field name="company_currency_id" column_invisible="True"/>
                </list>
            </field>
        </record>

        <!-- TRY tree view for ledger balance -->
        <record id="view_account_ledger_balance_tr_tree" model="ir.ui.view">
            <field name="name">account.ledger.balance.tr.tree</field>
            <field name="model">account.ledger.balance</field>
            <field name="arch" type="xml">
                <list string="Ledger Balance TL" create="false" delete="false" edit="false" js_class="ledger_balance">
                    <field name="partner_id" string="Partner"/>
                    <field name="balance_try" widget="monetary" options="{'currency_field': 'try_currency_id'}"/>
                    <field name="try_currency_id" column_invisible="True"/>
                </list>
            </field>
        </record>

        <!-- TRY action — all partners -->
        <record id="action_ledger_balance_tr" model="ir.actions.act_window">
            <field name="name">Ledger Balance TL</field>
            <field name="res_model">account.ledger.balance</field>
            <field name="view_mode">list</field>
            <field name="view_id" ref="view_account_ledger_balance_tr_tree"/>
            <field name="domain">[]</field>
            <field name="context">{'action_name': 'Ledger Balance in TRY'}</field>
        </record>

        <!-- TRY action — sale partners only -->
        <record id="action_sale_ledger_balance_tr" model="ir.actions.act_window">
            <field name="name">Sales Ledger Balance TL</field>
            <field name="res_model">account.ledger.balance</field>
            <field name="view_mode">list</field>
            <field name="view_id" ref="view_account_ledger_balance_tr_tree"/>
            <field name="domain">[('partner_id.sale_order_ids', '!=', False)]</field>
            <field name="context">{'is_sale_view': True, 'action_name': 'Ledger Balance in TRY'}</field>
        </record>

        <record id="action_partner_balance_menu_report" model="ir.actions.act_window">
            <field name="name">Partner Balance</field>
            <field name="res_model">account.ledger.balance</field>
            <field name="view_mode">list</field>
            <field name="view_id" ref="view_account_ledger_balance_tree"/>
            <field name="search_view_id" ref="view_account_ledger_balance_search"/>
            <field name="domain">[]</field>
            <field name="context">{}</field>
        </record>

        <menuitem
            id="menu_partner_balance_report"
            name="Ledger Balance"
            parent="account.account_reports_partners_reports_menu"
            action="action_partner_balance_menu_report"
            groups="account.group_account_manager,account.group_account_user"
            sequence="10"/>

        <record id="action_sale_partner_balance_report" model="ir.actions.act_window">
            <field name="name">Sales Ledger Balance</field>
            <field name="res_model">account.ledger.balance</field>
            <field name="view_mode">list</field>
            <field name="view_id" ref="view_account_ledger_balance_tree"/>
            <field name="search_view_id" ref="view_account_ledger_balance_search"/>
            <field name="domain">[('partner_id.sale_order_ids', '!=', False)]</field>
            <field name="context">{'is_sale_view': True, 'search_default_group_by_salesperson': 1}</field>
        </record>

        <menuitem
            id="menu_sale_partner_balance_report"
            name="Ledger Balance"
            parent="sale.menu_sale_report"
            action="action_sale_partner_balance_report"
            groups="account.group_account_manager,account.group_account_user,sales_team.group_sale_salesman"
            sequence="50"/>

    </data>
</odoo>
