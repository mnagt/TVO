<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_audit_cheque_outstanding" model="ir.actions.server">
        <field name="name">Audit: Cheque Outstanding Lines</field>
        <field name="model_id" ref="cheque.model_account_cheque"/>
        <field name="binding_model_id" ref="cheque.model_account_cheque"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
env['account.cheque'].action_audit_outstanding()
        </field>
    </record>
</odoo>
